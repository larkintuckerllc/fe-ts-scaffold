import Todo from './Todo';

describe('Todo', () => {

  it('get id', () => {
    const todoJS = {
      completed: true,
      id: 0,
      title: '',
      userID: 0,
    };
    const todo = new Todo(todoJS);
    const id = todo.get('id');
    expect(id).toBe(0);
  });

});
